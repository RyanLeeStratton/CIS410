using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Token : MonoBehaviour {

	/* At least one token should appear when an enemy dies */
	/* Have the token disappear after 5 seconds or so */
	/* If player collects it, increment coin counter by x */
	/* Destroy the clone */

}